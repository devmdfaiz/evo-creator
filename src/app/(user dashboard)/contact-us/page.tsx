"use client"
import { MainComponentsWrapperOnly } from "@/components/global/wrapper/MainComponentsWrapper";
import TypographyP from "@/components/typography/TypographyP";
import { ScrollArea } from "@/components/ui/scroll-area";
import React from "react";

const page = () => {
  return <MainComponentsWrapperOnly>
     <ScrollArea className="w-[40%] mx-auto h-screen bg-card">
      This page not yet build
     </ScrollArea>
  </MainComponentsWrapperOnly>;
};

export default page;
